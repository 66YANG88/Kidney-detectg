# nnUNet 肾脏囊性病变项目 —— 运行环境配置说明

## 一、环境概览

| 项目 | 内容 |
|------|------|
| 操作系统 | Linux Ubuntu 22.04 |
| Python | 3.10.16 |
| Conda 环境 | `torch`（位于 `/data/miniconda/envs/torch`）|
| PyTorch | 2.5.0+cu121（CUDA 12.1）|
| nnUNetv2 | 2.6.2（开发模式安装，位于 `/data/nnUNet-master`）|

---

## 二、首次配置步骤（已完成，仅供参考）

### 1. 激活 Conda 环境

```bash
conda activate torch
```

### 2. 安装 nnUNetv2（开发模式）

```bash
cd /data/nnUNet-master
pip install -e .
```

### 3. 安装 Streamlit（Web 应用依赖）

```bash
pip install streamlit
```

### 4. 持久化环境变量（已写入 `~/.bashrc`，**无需重复执行**）

```bash
# 以下内容已追加到 ~/.bashrc，每次开机自动生效
source /data/nnUNet-master/setup_env.sh
```

---

## 三、每次重新连接服务器后的快速配置

**只需执行以下两步：**

### 步骤 1：激活 Conda 环境

```bash
conda activate torch
```

### 步骤 2：加载 nnUNet 环境变量

```bash
source /data/nnUNet-master/setup_env.sh
```

> 如果 `~/.bashrc` 已自动 source，步骤 2 可跳过。可通过以下命令验证变量是否已设置：
> ```bash
> echo $nnUNet_raw
> # 输出: /data/nnUNet_data/nnUNet_raw
> ```

---

## 四、启动 Web 诊断应用

```bash
conda activate torch
cd /data/nnUNet-master
streamlit run app.py --server.port 8501 --server.address 0.0.0.0
```

浏览器访问：`http://<服务器IP>:8501`

---

## 五、训练模型

### 数据集信息

| 项目 | 内容 |
|------|------|
| 数据集名称 | Dataset001_KidneyCyst |
| 训练样本 | 79 个（63 训练 / 16 验证，5-fold 交叉验证）|
| 标签 | 0=背景，1=肾脏，2=肾脏囊肿 |
| 模态 | CT |
| 原始数据路径 | `/data/nnUNet_data/nnUNet_raw/Dataset001_KidneyCyst` |
| 预处理数据路径 | `/data/nnUNet_data/nnUNet_preprocessed/Dataset001_KidneyCyst` |
| 模型结果路径 | `/data/nnUNet_data/nnUNet_results/Dataset001_KidneyCyst` |

> **注意**：数据预处理已完成，无需重新执行 `nnUNetv2_plan_and_preprocess`。

### 启动训练命令

```bash
conda activate torch
source /data/nnUNet-master/setup_env.sh

# 训练 100 epoch（快速验证）
nnUNetv2_train 001 3d_fullres 0 -tr nnUNetTrainer_100epochs

# 训练 1000 epoch（完整训练，默认）
nnUNetv2_train 001 3d_fullres 0

# 从断点继续训练（加 --c 参数）
nnUNetv2_train 001 3d_fullres 0 --c
```

### 后台训练（防止终端断开导致中断）

```bash
nohup nnUNetv2_train 001 3d_fullres 0 -tr nnUNetTrainer_100epochs \
  > /data/nnUNet_data/train.log 2>&1 &

# 查看训练进度
tail -f /data/nnUNet_data/train.log
```

---

## 六、运行推理预测

```bash
conda activate torch
source /data/nnUNet-master/setup_env.sh

nnUNetv2_predict \
  -i <输入文件夹> \
  -o <输出文件夹> \
  -d 001 \
  -c 3d_fullres \
  -f 0
```

---

## 七、目录结构说明

```
/data/
├── nnUNet-master/          # 项目代码
│   ├── app.py              # Streamlit Web 应用
│   ├── setup_env.sh        # 环境变量设置脚本
│   ├── convert_data.py     # 数据转换脚本
│   └── nnunetv2/           # nnUNet 核心代码
└── nnUNet_data/            # 数据目录
    ├── nnUNet_raw/         # 原始数据
    ├── nnUNet_preprocessed/ # 预处理后数据
    └── nnUNet_results/     # 训练结果与模型
```

---

## 八、常见问题

**Q: 训练时提示找不到 nnUNet 环境变量？**  
A: 执行 `source /data/nnUNet-master/setup_env.sh`

**Q: `nnUNetv2_train` 命令找不到？**  
A: 确认已激活 `torch` 环境：`conda activate torch`

**Q: CUDA out of memory？**  
A: 在训练命令末尾添加 `--c` 从断点继续，或减小 batch size（需修改 plans.json）

**Q: Streamlit 端口被占用？**  
A: 更换端口：`streamlit run app.py --server.port 8502`
